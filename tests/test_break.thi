main :: () -> i32 {

    i := 0

    # The issue here is that were 
    # generating the code from the bottom of the tree
    # so the if statements has a continue trying to 
    # jump to a contiue label. That label hasnt been 
    # set up yet by the for loop. 
    # 
    # We need to do like LLVM and return an abstraction
    # of the call. Like CallInst, ValueInst

    b := 9
    for k: 0 .. b {

        if k == 3 { break }

        i += 1
    }

    ret i - 2
}

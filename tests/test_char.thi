extern puts (u8*) -> s32
extern putchar (s32) -> s32
extern malloc (s64) -> u8*
extern atoi (u8*) -> s32
extern exit (s32) -> u8*
extern system (u8*) -> s32

def main () -> s32 

    a : char* = "Hello"
    putchar(a[0])
    putchar(a[1])
    putchar(a[2])
    putchar(a[3])
    putchar(a[4])
    puts(a)

    # puts(a)
    # puts("This is the baseline. This must work.")
    ret 1

extern putchar (s32)  s32

EOF :: -1

puts(c: u8*)
    i := 0
    while c[i]
        if putchar(c[i]) == EOF return EOF
        i += 1
    if putchar('\n') == EOF return EOF

main() 
    a: u8* = "65"

    puts("Hello")
    puts("Titthei")
    puts("Tit thei")
    puts(a)

    return 1

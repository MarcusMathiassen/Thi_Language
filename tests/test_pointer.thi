# extern printf(u8*, ...) s32
# extern malloc(u64) u8*

# glfwGetFramebufferSize(width: s32*, height: s32*)
#     width[0] = 640
#     height[0] = 480


struct v2
    x: f32
    y: f32

get()
    return 1
    return 0

enum Kind
    Float :: 0
    Int :: 1

main(argc: s32, argv: char**)
    i : v2

    k: Kind = Kind.Int

    if k == .Float
        is Kind.Int
            return 1
        # is .Float return 1
        # is Kind.Float return 1

    # w: s32* = (malloc(4))
    # h: s32
    # glfwGetFramebufferSize(w, &h)
    # printf("framebuffer size: %dx%d\n", w[0], h)
    # return w[0] - h - 159
    return 0

load "includes/cstdio"

enum Mode
    dark :: 0
    light :: 1

main()

    i := 1 + 2

    printf("'i' has the value of %d\n", i)

    mode := Mode.light

    if mode
        is Mode.light fallthrough
        is Mode.dark puts("Dark!")

    return 1

def TokenKind enum
    TOKEN_IDENTIFIER
    TOKEN_CARMA

def v2 struct
    x f32
    y f32

def main(argc s32, argv u8**) s32
    
    extern printf(u8*, ...) s32

    for i = 0; i < argc; ;
        printf("%d = %s\n", i++, argv[i])
        #printf("%d = %s\n", i, *(argv+8*i))

    return 1

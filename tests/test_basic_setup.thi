
load "includes/sys"
#load "includes/cstdio"
#extern malloc(u64) u8*

def main(argc s32, argv u8**) s32

    __return_value__ s32 = 1


    for i = 0; i < argc; ++i
        print(argv[i])

    print("\n")
    
    for i in 0 .. argc
        print(argv[i])

    print("\n")

    #print("What's your name: ")
    #name = malloc(1024)
    #read(STDIN, name, 1024)
    #print("And age: ")
    #age = malloc(1024)
    #read(STDIN, age, 1024)

    #print("Hello, ")
    #print(name)
    #print("You are ")
    #printn(age, strlen(age)-2) # we remove the \n and \0
    #print(" years old\n")

    #x = 0.1
    #y = 0.2

    #printf("%.17f\n", x+y)
    #printf("%.17f\n", 0.1+0.2)
    #printf("0.1 + 0.2 = %f\n", 0.1 + 0.2)
    #printf("1.0/3.0 = %.17f\n", 1.0/3.0)

    exit(__return_value__)

def printn(msg u8*, n s64)
    write(STDOUT, msg, n)

def print(msg u8*)
    write(STDOUT, msg, strlen(msg))

def strlen(x u8*) s64 => i = x while *x++ {} x-i

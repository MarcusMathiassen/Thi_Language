def print(msg u8*)
    msg_len = strlen(msg)
    asm
        mov     rax, 0x2000004
        mov     rdi, 1        
        mov     rsi, %msg     
        mov     rdx, %msg_len 
        syscall

# Hello i am a comment
def main() s32
    print("Marcus Mathiassen\n")
    return 1

def strlen(x u8*) s64 => i = 0 while *(&x + i++) != 0 {} i

